export default function TodoCard({ todo, onToggle, onEdit, onDelete }) { return <article className={`todo-card ${todo.completed ? 'is-complete' : ''}`}><button className="todo-check" onClick={() => onToggle(todo)} aria-label={todo.completed ? 'Mark task pending' : 'Mark task complete'}>{todo.completed ? '✓' : ''}</button><div className="todo-copy"><h3>{todo.title}</h3>{todo.description && <p>{todo.description}</p>}<div className="todo-meta"><span className={`priority ${todo.priority}`}>{todo.priority}</span><span>{todo.category}</span>{todo.dueDate && <span>Due {new Date(todo.dueDate).toLocaleDateString()}</span>}<span>Added {new Date(todo.createdAt).toLocaleDateString()}</span></div></div><div className="todo-actions"><button onClick={() => onEdit(todo)} aria-label="Edit task">Edit</button><button onClick={() => onDelete(todo)} aria-label="Delete task">Delete</button></div></article>; }
